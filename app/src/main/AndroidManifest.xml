<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.InvestmentAndFinancialAnalysisSystem">

    <!-- android:allowBackup:用户可以对数据进行备份和修复
         android:icon:应用程序的图标
         android:label:应用程序的名字
         android:roundIcon:应用程序图标适配
         android:supportsRtl:
         android:theme:
         -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="投资分析理财系统"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--activity android:name=".Sign_in_Message"
                  android:parentActivityName=".MainActivity">
             The meta-data tag is required if you support API level 15 and lower -->
        <!-- <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />
    </activity>
        -->
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User"
            android:label="个人中心"/>
        <activity android:name=".Register"
            android:label="用户注册"/>
        <activity android:name=".CompletePersonalInformation"
            android:label="完善个人信息"/>
        <activity android:name=".Report"
            android:label="个人报告"/>
        <activity android:name=".NearlyPlan"
            android:label="近期的理财方案"/>
        <activity android:name=".Report2"
            android:label="个人报告"/>
    </application>

</manifest>